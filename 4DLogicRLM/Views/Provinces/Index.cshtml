@model IEnumerable<_4DLogicRLM.Models.Province>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProvinceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProvinceShortName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KMLFile)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProvinceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProvinceShortName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KMLFile)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProvinceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProvinceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProvinceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
