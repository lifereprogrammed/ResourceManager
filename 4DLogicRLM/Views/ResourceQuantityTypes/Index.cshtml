@model IEnumerable<_4DLogicRLM.Models.ResourceQuantityType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ResournceQuantityTypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResournceQuantityTypeDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResourceQuantityTypeShortName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResournceQuantityTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResournceQuantityTypeDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResourceQuantityTypeShortName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResourceQuantityTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResourceQuantityTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResourceQuantityTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
